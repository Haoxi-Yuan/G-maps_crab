# Exploratory code

`image-analysis/` contains menu-image manifests, OCR crop generation, Luna and
PaddleOCR pilots, video-type checks, and their focused tests. These tools are
not called by the five-stage production CLI.

Promotion into the core requires deterministic fixtures, supported dependency
installation, a documented command contract, and cross-platform CI coverage.
